import type { Category } from '../types/sales'

// teal / blue / brown — same ones on every chart
export const CATEGORY_COLORS: Record<Category, string> = {
  Electronics: '#0d9488',
  Clothing: '#0369a1',
  Furniture: '#b45309',
}

